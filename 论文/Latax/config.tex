\usepackage[utf8]{inputenc}
\usepackage{mathptmx}
\usepackage{setspace}
\usepackage[hidelinks]{hyperref}
\usepackage{booktabs}
\usepackage[a4paper,margin=1in]{geometry}
\usepackage[most]{tcolorbox}
\usepackage{xcolor}
\usepackage{newtxtext,newtxmath} % Times 风格字体
\definecolor{boxblue}{RGB}{48,105,152}
\setlength{\parindent}{0pt}
\usepackage[style=apa,backend=biber]{biblatex}
\addbibresource{ref.bib}

\usepackage{tikz}
\usetikzlibrary{shapes.geometric, arrows, positioning, fit, calc}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usepgfplotslibrary{colorbrewer}  % unlocks ColorBrewer colormaps (Blues, Reds, ...)
\usepackage{multirow}
\usepackage{algorithm}
\usepackage{algpseudocode}

\geometry{
 a4paper,
 top=2.54cm,
 bottom=2.54cm, 
 left=3.17cm,
 right=2.79cm,
}

\onehalfspacing
\renewcommand{\familydefault}{\rmdefault}

\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{} 
\renewcommand{\headrulewidth}{0pt} 
\fancyhead[C]{\fontsize{12pt}{14pt}\selectfont XIAMEN UNIVERSITY MALAYSIA}
\fancyfoot[C]{\thepage}
\setlength{\headheight}{15pt}
\setlength{\headsep}{21pt}


% =====================================================================
% --- Gap 2 dependencies (add these if config.tex doesn't already have them) ---
% Safe to load even if config.tex already loads them; LaTeX silently
% skips a duplicate \usepackage.
\usepackage{float}                          % gives float specifier [H]
\usetikzlibrary{positioning}                % enables "right=14mm of cfg" syntax
\usetikzlibrary{fit}                        % enables fit=(node)(node)
\usetikzlibrary{backgrounds}                % enables [on background layer]
\usetikzlibrary{calc}                       % enables ($(...)+(...)$) coord math
\usetikzlibrary{arrows.meta}                % enables -{Latex[length=2mm]}
\usetikzlibrary{decorations.pathreplacing}  % enables decoration={brace,...}

\setlength{\parindent}{0pt}
\setlength{\parskip}{6pt}

% --- TikZ styles used by the Gap 2 figures ---
\tikzset{
  gap2block/.style={rectangle, draw, rounded corners=2pt, align=center,
                    minimum height=8mm, inner sep=4pt, font=\small},
  gap2ghost/.style={gap2block, dashed, fill=red!5, draw=red!60},
  gap2ok/.style   ={gap2block, fill=green!8, draw=green!50!black},
  gap2warn/.style ={gap2block, fill=orange!10, draw=orange!70!black},
  gap2arr/.style  ={-{Latex[length=2mm]}, thick},
  gap2brace/.style={decorate, decoration={brace, amplitude=4pt}},
}
% =====================================================================

% --- 各类子标题换行，字体设置 ---
\usepackage{titlesec}

\renewcommand{\theparagraph}{\thesubsubsection.\arabic{paragraph}}

\titleformat{\paragraph}
  {\large\bfseries}    
  {\theparagraph}            
  {1em}                      
  {}                        
  []                         % 强制换行
\titlespacing*{\paragraph}
  {0pt}{1.5ex plus .5ex}{1ex}  


\titleformat{\subsubsection}
  {\large\bfseries}    
  {\thesubsubsection}  
  {1em}                
  {}